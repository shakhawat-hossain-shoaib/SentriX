# ==========================================================
# SentriX - Campus Cybersecurity Incident Platform Environment
# ==========================================================

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=sentrix-campus-cybersecurity-secret-key-2026

# MySQL Database Configuration (Optional: automatically falls back to SQLite if inactive)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sentrix_db

# Frontend URL (CORS)
CLIENT_URL=http://localhost:3000
